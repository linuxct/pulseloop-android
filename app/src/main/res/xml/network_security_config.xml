<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext is permitted so a user-configured http:// OTLP endpoint (e.g. a self-hosted
         Grafana Alloy on the LAN) works — the app trusts the user's explicit endpoint choice.
         For https:// endpoints, OtlpHttpSender enforces strict TLS (MODERN_TLS, no trust-all
         bypass), so a bad/self-signed cert is rejected. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Keep the OpenAI API HTTPS-only regardless of the relaxed base above. -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.openai.com</domain>
    </domain-config>
</network-security-config>
